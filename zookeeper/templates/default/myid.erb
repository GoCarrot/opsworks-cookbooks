<%= node[:opsworks][:layers][:zookeeper][:instances].map {|instance_name, instance_config| instance_config["private_ip"]}.push(node[:opsworks][:instance][:private_ip].uniq.sort.index(node[:opsworks][:instance][:private_ip]) + 1 %>
